<div class="container">
	<div class="jumbotron">
		{{ form_start(form) }}
		{{ form_widget(form.content) }}
		{{ form_widget(form.save, { label : submit_label } )}}
		{{ form_end(form) }}
	</div>
</div>
